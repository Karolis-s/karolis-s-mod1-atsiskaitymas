from setuptools import setup, find_packages

setup(
    name ="karolis_s_mod1_atsiskaitymas",
    version ="1.0.0",
    packages = find_packages(),
    install_requires=[
        #pip install requests, lxml.html, csv
    ]
)